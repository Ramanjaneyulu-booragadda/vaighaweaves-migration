/**
 * CodPayment — placeholder component for Cash on Delivery.
 *
 * Wire up to the `pp_cod_*` Medusa payment provider when it is added.
 */
export function CodPayment() {
  return (
    <p className="text-sm text-gray-600">
      Payment will be collected at delivery.
    </p>
  )
}
